---
description: 本地开发不得影响线上环境（sztu.lingubot.cn）
alwaysApply: true
---

# 禁止影响线上环境

线上地址：https://sztu.lingubot.cn

## 禁止操作

- 不得执行部署、发布、上传构建产物到线上服务器
- 不得 `git push` 到可能触发线上部署的分支，除非用户明确要求
- 不得修改线上服务器配置、Nginx、CI/CD 流水线
- 不得运行可能写入线上数据的脚本或批量 API 调用（增删改）
- 不得修改 `.env.prod` 等生产环境配置用于本地直连写操作

## 允许操作

- 本地 `pnpm dev` 开发，改动仅在本机生效
- 只读地查看线上 API 响应（GET 请求）用于对照调试
- 本地 mock 数据或本地/测试后端替代线上 API

## 涉及写操作时

若任务需要测试登录、提交表单、删除数据等写操作，先提醒用户风险，并优先建议：
1. 使用 mock / 本地后端
2. 使用独立测试环境
3. 用户明确确认后再对线上 API 执行写操作
